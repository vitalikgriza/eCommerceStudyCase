import React from 'react';
import { ActivityIndicator, StyleSheet } from 'react-native';

const AppLoading = () => {
  return <ActivityIndicator size="large" style={StyleSheet.absoluteFillObject} />;
};

export default AppLoading;
